# Test fixtures
- `rp-calcs.json` — calcs captured from the live site (engine parity net)
- `saves/*.sav` (gitignored) + `saves/*.expected.json` — save-parser net
Do NOT commit real save files if they contain anything personal; commit the
expected-JSON only and keep the .sav local.
